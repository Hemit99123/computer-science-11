
# only need one function so just import that not the whole lib (saving computation costs)

from random import randint

# the upper and lower limits are inclusive, meaning they could be producec

RANDOM_NUMBER = randint(1,10)

